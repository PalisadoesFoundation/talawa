name: Count Lines 
on: [pull_request]

jobs:
  count-lines-of-code:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: git ls-files "*.dart" |  xargs wc -l | awk '$1 > 400' | if wc -l > 1; then exit 0; else exit 1;   