name: Count Lines 
on: [pull_request]

jobs:
  count-lines-of-code:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - run: git ls-files "*.dart" | xargs wc -l | awk '$1 > 500' | awk 'END{print (NR==1)?1:0}'   