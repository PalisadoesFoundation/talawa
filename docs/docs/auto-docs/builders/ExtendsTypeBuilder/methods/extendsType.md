# Method: `extendsType`

## Description

Sets the `extends` clause to [superclass].

 The type must not already have an `extends` clause.

## Return Type
`void`

## Parameters

- `superclass`: `NamedTypeAnnotationCode`
