# Method: `timerPickerHourLabel`

## Description

Label that appears next to the hour picker in
 [CupertinoTimerPicker] when selected hour value is `hour`.
 This function will deal with pluralization based on the `hour` parameter.

## Return Type
`String?`

## Parameters

- `hour`: `int`
