# Method: `ensureBranchPointTagged`

## Description

Ensures the branch point [candidateBranch] and `master` has a version tag.

 This is necessary for version reporting for users on the `master` channel
 to be correct.

## Return Type
`Future&lt;Version&gt;`

## Parameters

- ``: `dynamic`
- ``: `dynamic`
- ``: `dynamic`
