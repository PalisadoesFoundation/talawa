# Method: `fseIdenticalSync`

## Description

Returns `true` if [path1] and [path2] are paths to the
 same file system object.

 When this override is installed, this function overrides the behavior of
 `FileSystemEntity.identicalSync`.

## Return Type
`bool`

## Parameters

- `path1`: `String`
- `path2`: `String`
