# Method: `fsWatchIsSupported`

## Description

Returns `true` when [FileSystemEntity.watch] is supported.

 When this override is installed, this function overrides the behavior of
 `FileSystemEntity.isWatchSupported`.

## Return Type
`bool`

