# Method: `updateAll`

## Description

Updates all values.

 Iterates over all entries in the map and updates them with the result
 of invoking [update].
 ```dart
 final terrestrial = <int, String>;
 terrestrial.updateAll((key, value) => value.);
 print(terrestrial); // 
 ```

## Return Type
`void`

## Parameters

- ``: `dynamic`
