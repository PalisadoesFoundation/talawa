# Method: `fuse`

## Description

Fuses `this` with [other].

 Encoding with the resulting converter is equivalent to converting with
 `this` before converting with `other`.

## Return Type
`Converter&lt;S, TT&gt;`

## Parameters

- `other`: `Converter&lt;T, TT&gt;`
