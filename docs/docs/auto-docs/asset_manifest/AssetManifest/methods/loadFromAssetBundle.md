# Method: `loadFromAssetBundle`

## Description

Loads asset manifest data from an [AssetBundle] object and creates an
 [AssetManifest] object from that data.

## Return Type
`Future&lt;AssetManifest&gt;`

## Parameters

- `bundle`: `AssetBundle`
