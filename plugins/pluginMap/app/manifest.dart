import 'package:talawa/plugin/types.dart';

/// Plugin manifest for Plugin Map
const pluginMapManifest = PluginManifest(
  id: 'plugin_map',
  name: 'Plugin Map (App)',
  description: 'Docs: menu injector + route extension demo',
);
